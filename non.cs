// Check if the length of the word exceeds the specified word length
if (word.Length > wordLength)
{
    // Perform the desired action if the condition is true
    // For example, let's say we want to print the word
    Console.WriteLine("The word is longer than the specified length: " + word);
}
else
{
    // Optionally, handle the case when the condition is false
    Console.WriteLine("The word does not exceed the specified length.");
}
